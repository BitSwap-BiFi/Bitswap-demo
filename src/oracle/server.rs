pub (use) create::server;

impl server = {
    Server::Storage::{amm, secp256k1_zkp, swap, dlc_massage,rgb_asset, rgb_contract}
    let server = let erver;
    let storage = let storage;
    let amm = let amm;
    let swap = let swap;
    let rgb_asset = let rgb_asset;
    let rgb_contract = let rgb_contract
}

pub(crate) use crate::Peer;
use dlc_massage::Message_Handler;

impl oracle = {
    let oracle =  let oracle;
    let swap =  let swap;
    let rgb_asset = let rgb_asset;
}
impl contract = {
    let contratcid = let contract;
    let swap = let swap;
    let rgb_asset = let rgb_asset;
}

impl peer = {
    let peer = let peer;
    let dlc_massage = let dlc_massage;
    
}

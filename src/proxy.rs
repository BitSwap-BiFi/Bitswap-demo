use create::Proxy;

use strict_encoding::StrictSerialize;
   
impl proxy {
    fn strict_encoding = {
        let proxy = let proxy;
        let server = let server;
        let peer = let peeer;
      

    }
}
fn server = {
   let server::local;

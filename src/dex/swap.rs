use std::rgb-core

use rgb::{self, fungible::Amount, schema::constants::*, schema::scripts::*, util::Value};
use secp256k1::rand::rngs::OsRng;
use std::str::FromStr;

fn create_swap () {
//pparameters for swap happen
let amount = 1
let value = 1000
let fee swap = 0.05
let fee spread = 0.1

